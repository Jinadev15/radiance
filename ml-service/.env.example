HOST=0.0.0.0
PORT=8000
COSINE_THRESHOLD=0.363
LIVENESS_LAPLACIAN_THRESHOLD=12.0
LIVENESS_GLARE_RATIO=0.15
LIVENESS_MIN_MOTION=2.0

# Optional, comma-separated. Not load-bearing (only the Node backend calls
# this service, server-to-server, which CORS doesn't apply to) — defense
# in depth only. Leave unset unless something else needs to call this
# service directly from a browser.
EXTRA_CORS_ORIGINS=
